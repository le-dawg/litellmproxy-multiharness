[project]
name = "litellm-simplified"
version = "0.2.0"
description = "Hardened LiteLLM proxy for Azure OpenAI GPT-5.4 with launchd daemon and parameter sanitization"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "litellm[proxy]>=1.89.0",
    "fastapi<0.140.0",
    "pyyaml>=6.0",
    "httpx>=0.27.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
